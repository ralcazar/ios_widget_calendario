Done. Here's a summary of what was implemented:

**CAL-sue.1 — Complete**

- **Xcode project** created at `Calendario/Calendario.xcodeproj` with:
  - `Calendario` app target (bundle: `com.ralcazar.calendario`, iOS 17+, SwiftUI)
  - `CalendarioWidget` extension target (bundle: `com.ralcazar.calendario.widget`)
  - `CalendarioTests` unit test bundle
- **App Groups** configured on both targets via entitlements files (`group.com.ralcazar.calendario`)
- **`SharedConstants.swift`** with `AppGroup` enum shared across app + widget targets
- **4 tests** all passing — covering identifier format, `UserDefaults` not nil, read/write roundtrip, and multiple key roundtrip
